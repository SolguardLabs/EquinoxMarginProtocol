name: CI

on:
    push:
    pull_request:

jobs:
    hardhat:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v7
              with:
                  node-version: 22
            - run: npm install
            - run: bash scripts/ci.sh
